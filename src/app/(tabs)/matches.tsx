import { PlaceholderScreen } from '@/components/ui/placeholder-screen';

export default function MatchesScreen() {
  return (
    <PlaceholderScreen
      title="Matches"
      message="Upcoming, live, and completed matches will appear here."
    />
  );
}
